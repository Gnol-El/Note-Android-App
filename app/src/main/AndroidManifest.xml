<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.noteappdemo">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteAppDemo">
        <activity
            android:name=".memorize.UpdateMemorizeActivity"
            android:exported="false" />
        <activity
            android:name=".event.UpdateEventActivity"
            android:exported="false" />
        <activity
            android:name=".memorize.CreateMemorizeActivity"
            android:exported="false" />
        <activity
            android:name=".memorize.MemorizeActivity"
            android:exported="false" />
        <activity
            android:name=".event.EventActivity"
            android:exported="false" />
        <activity
            android:name=".OptionsActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>